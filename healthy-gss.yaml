apiVersion: game.kruise.io/v1alpha1
kind: GameServerSet
metadata:
  name: test-healthy
  namespace: default
spec:
  replicas: 2
  updateStrategy:
    rollingUpdate:
      podUpdatePolicy: InPlaceIfPossible
  gameServerTemplate:
    spec:
      containers:
        - image: nginx:1.23
          name: game-server
